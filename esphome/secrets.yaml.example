# Kopiere nach secrets.yaml (gleicher Ordner) und Werte eintragen.
# secrets.yaml nicht versionieren.

wifi_ssid: "DEIN_WLAN"
wifi_password: "DEIN_PASSWORT"

# openssl rand -base64 32
api_encryption_key: "GENERIEREN_UND_EINTRAGEN"

ota_password: "SICHERES_OTA_PASSWORT"

# Optional: nur nötig, wenn die Zähler-Entity in HA anders heißt → in soyosource-victron-esp32.yaml
#   homeassistant_power_entity: !secret ha_grid_power_entity
# ha_grid_power_entity: sensor.electric_meter_ir_active_power
