# Home Assistant package: Waste collection schedule
#
# Preconditions:
# - Home Assistant loads packages from homeassistant/packages/
# - The waste_collection_schedule integration (HACS) is installed

waste_collection_schedule:
  sources:
    - name: ics
      args:
        url: !secret waste_ics_url
  sensors:
    - name: muell
